"""FastAPI routes — REST API surface for Phase 1.

Routes (per BACKEND_ARCHITECTURE_PHASE1.md §4):
- /projects, /projects/{id}, /projects/{id}/status, /reports, etc.
UI is deferred — these are exercised via curl/Postman in Phase 1.
"""
